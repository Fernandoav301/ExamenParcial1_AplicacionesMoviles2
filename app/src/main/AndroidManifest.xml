<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cuatrimestre6_parcial1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cuatrimestre6_Parcial1">
        <activity android:name=".Pantalla_neptuno"></activity>
        <activity android:name=".pantalla_urano" />
        <activity android:name=".pantalla_saturno" />
        <activity android:name=".pantalla_jupiter" />
        <activity android:name=".pantalla_marte" />
        <activity android:name=".pantalla_tierra" />
        <activity android:name=".Pantalla_venus" />
        <activity android:name=".pantalla_mercurio" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>